{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core APIs and file dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
